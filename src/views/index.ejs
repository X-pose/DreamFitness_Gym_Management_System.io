<!--This is the entry point to the frontend from the server side
in here, we serves the index.jsx or bundle.js(bundled compiled code of the react app) to the client browser.
Then the browser downloads the file and executes it
-->
